import React from "react";

const FilterPanel: React.FC = () => (
  <aside style={{ marginTop: "20px", opacity: 0.6 }}>
    <p>Filter & Bookmark options (coming soon)</p>
  </aside>
);

export default FilterPanel;
